<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blitz.app">

    <application
        android:name=".base.application.BaseApplication"
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <!-- Facebook SDK appId used for authentication. -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/app_id" />

        <!-- Facebook Login activity -->
        <activity
            android:name="com.facebook.LoginActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <activity android:name=".screens.splash.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".screens.main.MainScreenActivity" />
        <activity
            android:name=".screens.post.PostScreenActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".screens.helper_found.HelperFoundScreen" />

    </application>

</manifest>
