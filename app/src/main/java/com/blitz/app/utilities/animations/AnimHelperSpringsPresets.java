package com.blitz.app.utilities.animations;

/**
* Created by Miguel on 7/30/2014.
*/
public enum AnimHelperSpringsPresets {

    // Slide down from outside of the screen
    // into the current position.
    SLIDE_DOWN,
    SLIDE_DOWN_REVERSED,

    // Slide up from under the screen
    // into the current position.
    SLIDE_UP,
    SLIDE_UP_REVERSED,

    // Slide right from left of screen
    // into the current position.
    SLIDE_RIGHT,
    SLIDE_RIGHT_REVERSED,

    // Slide from the right of screen
    // into the current position.
    SLIDE_LEFT,
    SLIDE_LEFT_REVERSED
}
